#include "AppHdr.h"

#include "enum.h"
#include "preparation-data.h"

// void init_preparation_index()
// {
//     for (unsigned int i = PREP_FIRST_PREPARATION; i <= PREP_LAST_PREPARATION; ++i)
//     {
//         unsigned int index = i - PREP_FIRST_PREPARATION;
//         preparation_data_map[static_cast<preparation_type>(i)] = &preparation_data[index];
//     }
// }
